---
title: useFlagsStatus
description: Check initialization status of feature flags.
icon: i-ph-heartbeat-duotone
---

Returns reactive status information about the feature flag system.

## Usage

```vue
<template>
  <div v-if="!ready">Loading feature flags...</div>
  <div v-else>{{ flagCount }} flags loaded</div>
</template>

<script setup>
const { ready, flagCount } = useFlagsStatus()
</script>
```

## Type

```ts
function useFlagsStatus(): {
  ready: ComputedRef<boolean>
  flagCount: ComputedRef<number>
}
```

## Returns

::field-group
  ::field{name="ready" type="ComputedRef<boolean>"}
  `true` after the first successful flag fetch. `false` if the Unleash proxy is unreachable.
  ::

  ::field{name="flagCount" type="ComputedRef<number>"}
  Number of loaded feature flags.
  ::
::
